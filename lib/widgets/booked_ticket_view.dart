import 'package:flutter/material.dart';

class BookedTicketView extends StatelessWidget {
  const BookedTicketView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Booked Ticket View');
  }
}